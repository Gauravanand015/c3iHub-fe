import React from "react";
import { useSelector } from "react-redux";
import Items from "../components/Items";

const Home = () => {
  return (
    <div>
      <Items />
    </div>
  );
};

export default Home;
